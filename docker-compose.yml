services:
  gitter:
    build:
      context: .
      dockerfile: Dockerfile
    image: gitter-cli:latest
    container_name: gitter-demo
    volumes:
      # Mount a sample project directory
      - ./docker-demo:/demo
    working_dir: /demo
    stdin_open: true
    tty: true

